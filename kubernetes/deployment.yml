apiVersion : apps/v1
kind: Deployment
metadata:
  name: sampleapp 
spec:
  progressDeadlineSeconds: 800
  replicas: #replicaNo#
  selector:
    matchLabels:
      app: sampleapp
  template:
    metadata:
      labels:
        app: sampleapp 
    spec:
      containers:
        - name: sampleapp 
          image: jhondeploytoaks21.azurecr.io/sampleapp
          ports:
          - containerPort: 80
